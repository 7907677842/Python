# Find largest number
def largestElement(arr: [], n: int) -> int:

    # Write your code from here.
    largest=0
    for i in range(n):
        if arr[i]>largest:
            largest=arr[i]
    return largest 
